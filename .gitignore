# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/

# Node
node_modules/

# Flux database (SQLite) — never commit user data
server/data.db
server/data.db-*
server/*.db
server/*.db-shm
server/*.db-wal
